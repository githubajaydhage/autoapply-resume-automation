name: Job Application - Ajay Dhage (DevOps/SRE)

# =================================================================
# AJAY DHAGE - DevOps Engineer / Site Reliability Engineer
# Branch: v.1.3.0-ajay
# =================================================================
# v7 FEATURES (Latest):
# ✅ Multi-Job Workflow (each phase visible in UI, can cancel individually)
# ✅ No Hardcoded Values (all config from JOB_KEYWORDS)
# ✅ Recruiting Agencies (50+ staffing firms)
# ✅ Fast Referrals (5-15s delays, 10 max)
# =================================================================
# ONLY 1 SECRET NEEDED: SENDER_PASSWORD_AJAY (Gmail App Password)
# =================================================================

on:
  workflow_dispatch:
    inputs:
      job_location:
        description: 'Job Location'
        required: true
        default: 'Bangalore'
        type: choice
        options:
        - Bangalore
        - Mumbai
        - Delhi
        - Hyderabad
        - Pune
        - Chennai
        - Remote
        - India
      max_emails:
        description: 'Max new emails to send'
        required: true
        default: '15'
        type: choice
        options:
        - '5'
        - '10'
        - '15'
        - '20'
        - '30'
        - '50'
      send_followups:
        description: 'Send follow-up emails?'
        required: false
        default: 'true'
        type: choice
        options:
        - 'true'
        - 'false'
      scrape_only:
        description: 'Only scrape (no emails)'
        required: false
        default: 'false'
        type: choice
        options:
        - 'false'
        - 'true'
      include_portfolio_links:
        description: 'Include GitHub/Portfolio links?'
        required: false
        default: 'true'
        type: choice
        options:
        - 'true'
        - 'false'
      send_slack_notifications:
        description: 'Slack notifications?'
        required: false
        default: 'true'
        type: choice
        options:
        - 'true'
        - 'false'
      enable_mobile_alerts:
        description: 'WhatsApp/Telegram alerts?'
        required: false
        default: 'false'
        type: choice
        options:
        - 'false'
        - 'true'
      enable_whatsapp:
        description: 'Enable WhatsApp (requires WHATSAPP_PHONE, CALLMEBOT_API_KEY secrets)'
        required: false
        default: 'false'
        type: choice
        options:
        - 'false'
        - 'true'
      enable_telegram:
        description: 'Enable Telegram (requires TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID secrets)'
        required: false
        default: 'false'
        type: choice
        options:
        - 'false'
        - 'true'
        
  schedule:
    # Runs 3 times per day (IST: 9:30 AM, 2:30 PM, 7:30 PM)
    - cron: '0 4,9,14 * * *'

# ============================================
# 🎯 CENTRALIZED USER CONFIGURATION
# All scripts read from these environment variables
# ============================================
env:
  # User Identity
  APPLICANT_NAME: 'Ajay Dhage'
  APPLICANT_FIRST_NAME: 'Ajay'
  APPLICANT_LAST_NAME: 'Dhage'
  APPLICANT_EMAIL: 'ajaykumardhage@gmail.com'
  SENDER_EMAIL: 'ajaykumardhage@gmail.com'
  APPLICANT_PHONE: '+91-9876543210'
  APPLICANT_LOCATION: 'Bangalore, Karnataka, India'
  APPLICANT_CITY: 'Bangalore'
  APPLICANT_COUNTRY: 'India'
  APPLICANT_LINKEDIN: 'https://www.linkedin.com/in/ajaydhage/'
  APPLICANT_GITHUB: 'https://github.com/githubajaydhage'
  APPLICANT_EXPERIENCE: '8'
  YEARS_EXPERIENCE: '8'
  APPLICANT_TARGET_ROLE: 'DevOps Engineer, Site Reliability Engineer, Platform Engineer'
  APPLICANT_SKILLS: 'Kubernetes, Docker, AWS, Azure, Terraform, Jenkins, GitLab CI, ArgoCD, Prometheus, Grafana'
  
  # Resume Configuration
  RESUME_FILENAME: 'Ajay_Dhage_Resume.pdf'
  RESUME_PATH: 'resumes/Ajay_Dhage_Resume.pdf'
  
  # ⚠️ JOB KEYWORDS - MOST IMPORTANT!
  JOB_KEYWORDS: 'devops, kubernetes, aws, azure, cloud engineer, sre, site reliability, platform engineer, infrastructure engineer, cicd, jenkins, terraform, ansible, docker'
  NAUKRI_KEYWORDS: 'devops engineer, kubernetes, aws cloud engineer, sre'
  
  # Optional Settings
  SEND_TO_AGENCIES: 'true'
  MAX_REFERRAL_REQUESTS: '10'
  MAX_REFERRALS_PER_COMPANY: '1'

jobs:
  # ============================================
  # JOB 1: SETUP & SCRAPE JOBS
  # ============================================
  scrape-jobs:
    name: "🔍 Phase 1: Scrape Jobs"
    runs-on: ubuntu-latest
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    
    outputs:
      jobs_count: ${{ steps.count.outputs.jobs }}
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
      with:
        ref: v.1.3.0-ajay

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Restore Previous Data
      uses: actions/cache@v3
      with:
        path: |
          data/sent_emails_log.csv
          data/followup_log.csv
          data/all_hr_emails.csv
          data/curated_hr_emails.csv
        key: job-data-ajay-${{ runner.os }}-v8
        restore-keys: |
          job-data-ajay-${{ runner.os }}-

    - name: Scrape Jobs (RemoteOK, Arbeitnow, Jobicy, Adzuna)
      run: |
        echo "🔍 Scraping jobs from multiple sources..."
        python scripts/reliable_job_scraper.py
      env:
        PYTHONPATH: ${{ github.workspace }}
        JOB_LOCATION: ${{ github.event.inputs.job_location || 'Bangalore' }}

    - name: Scrape Naukri.com
      run: |
        echo "🇮🇳 Scraping Naukri.com..."
        python scripts/naukri_scraper.py || true
      env:
        PYTHONPATH: ${{ github.workspace }}
        NAUKRI_LOCATION: ${{ github.event.inputs.job_location || 'bangalore' }}
        NAUKRI_EXPERIENCE: 'senior'

    - name: Scrape Enhanced Sources
      run: |
        echo "🚀 Scraping Wellfound, LinkedIn, etc..."
        python scripts/enhanced_job_scraper.py || true
      env:
        PYTHONPATH: ${{ github.workspace }}

    - name: Count Jobs
      id: count
      run: |
        if [ -f data/jobs_today.csv ]; then
          JOBS=$(($(wc -l < data/jobs_today.csv) - 1))
          echo "jobs=$JOBS" >> $GITHUB_OUTPUT
          echo "✅ Found $JOBS jobs"
        else
          echo "jobs=0" >> $GITHUB_OUTPUT
        fi

    - name: Upload Scraped Jobs
      uses: actions/upload-artifact@v4
      with:
        name: scraped-jobs
        path: |
          data/jobs_today.csv
          data/naukri_jobs.csv
          data/enhanced_jobs.csv
          data/sent_emails_log.csv
          data/followup_log.csv
        retention-days: 1

  # ============================================
  # JOB 2: DISCOVER HR EMAILS
  # ============================================
  discover-hr:
    name: "📧 Phase 2: Find HR Emails"
    runs-on: ubuntu-latest
    needs: scrape-jobs
    
    outputs:
      emails_count: ${{ steps.count.outputs.emails }}
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
      with:
        ref: v.1.3.0-ajay

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Download Scraped Jobs
      uses: actions/download-artifact@v4
      with:
        name: scraped-jobs
        path: data/

    - name: Load Curated HR Database
      run: |
        echo "📚 Loading 170+ verified HR emails..."
        python scripts/curated_hr_database.py
      env:
        PYTHONPATH: ${{ github.workspace }}

    - name: Find HR Emails from Job Postings
      run: |
        echo "🔍 Finding HR emails..."
        python scripts/hr_email_finder.py
        python scripts/email_scraper.py || true
      env:
        PYTHONPATH: ${{ github.workspace }}

    - name: Resume Optimization Analysis
      run: |
        echo "📄 Analyzing resume match scores..."
        python scripts/resume_optimizer.py || true
      env:
        PYTHONPATH: ${{ github.workspace }}

    - name: Count HR Emails
      id: count
      run: |
        if [ -f data/all_hr_emails.csv ]; then
          EMAILS=$(($(wc -l < data/all_hr_emails.csv) - 1))
          echo "emails=$EMAILS" >> $GITHUB_OUTPUT
          echo "✅ Found $EMAILS HR emails"
        else
          echo "emails=0" >> $GITHUB_OUTPUT
        fi

    - name: Upload HR Data
      uses: actions/upload-artifact@v4
      with:
        name: hr-emails
        path: |
          data/
        retention-days: 1

  # ============================================
  # JOB 3: SEND APPLICATION EMAILS
  # ============================================
  send-emails:
    name: "✉️ Phase 3: Send Emails"
    runs-on: ubuntu-latest
    needs: discover-hr
    if: ${{ github.event.inputs.scrape_only != 'true' }}
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
      with:
        ref: v.1.3.0-ajay

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Download HR Data
      uses: actions/download-artifact@v4
      with:
        name: hr-emails
        path: data/

    - name: Generate Cover Letters
      run: |
        echo "📝 Generating personalized cover letters..."
        python scripts/cover_letter_generator.py || true
      env:
        PYTHONPATH: ${{ github.workspace }}

    - name: Send Application Emails
      run: |
        echo "📤 Sending application emails (max: ${{ github.event.inputs.max_emails || '15' }})..."
        python scripts/email_sender.py
      env:
        PYTHONPATH: ${{ github.workspace }}
        SENDER_PASSWORD: ${{ secrets.SENDER_PASSWORD_AJAY }}
        MAX_EMAILS: ${{ github.event.inputs.max_emails || '15' }}
        INCLUDE_PORTFOLIO_LINKS: ${{ github.event.inputs.include_portfolio_links || 'true' }}

    - name: Upload Email Results
      uses: actions/upload-artifact@v4
      with:
        name: email-results
        path: |
          data/
          cover_letters/
        retention-days: 1

  # ============================================
  # JOB 4: SEND REFERRAL REQUESTS
  # ============================================
  send-referrals:
    name: "🤝 Phase 4: Referrals"
    runs-on: ubuntu-latest
    needs: send-emails
    if: ${{ github.event.inputs.scrape_only != 'true' }}
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
      with:
        ref: v.1.3.0-ajay

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Download Email Results
      uses: actions/download-artifact@v4
      with:
        name: email-results
        path: data/

    - name: Send Referral Requests (10x response rate!)
      run: |
        echo "🤝 Sending referral requests..."
        echo "   Max: $MAX_REFERRAL_REQUESTS referrals"
        python scripts/referral_system.py || true
      env:
        PYTHONPATH: ${{ github.workspace }}
        SENDER_PASSWORD: ${{ secrets.SENDER_PASSWORD_AJAY }}

    - name: Detect HR Replies
      run: |
        echo "📬 Scanning inbox for HR replies..."
        python scripts/reply_detector.py || true
      env:
        PYTHONPATH: ${{ github.workspace }}
        SENDER_PASSWORD: ${{ secrets.SENDER_PASSWORD_AJAY }}

    - name: Upload Referral Results
      uses: actions/upload-artifact@v4
      with:
        name: referral-results
        path: |
          data/
        retention-days: 1

  # ============================================
  # JOB 5: FOLLOW-UPS & BOUNCE CHECKING
  # ============================================
  followups:
    name: "🔄 Phase 5: Follow-ups"
    runs-on: ubuntu-latest
    needs: send-referrals
    if: ${{ github.event.inputs.scrape_only != 'true' && github.event.inputs.send_followups != 'false' }}
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
      with:
        ref: v.1.3.0-ajay

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Download Referral Results
      uses: actions/download-artifact@v4
      with:
        name: referral-results
        path: data/

    - name: Send Multi-Stage Follow-Ups
      run: |
        echo "🔄 Sending follow-ups (Day 3, 7, 14)..."
        python scripts/followup_sender.py
      env:
        PYTHONPATH: ${{ github.workspace }}
        SENDER_PASSWORD: ${{ secrets.SENDER_PASSWORD_AJAY }}
        MAX_FOLLOWUPS: '15'

    - name: Check Email Bounces
      run: |
        echo "📬 Checking for bounced emails..."
        python scripts/bounce_checker.py || true
        python scripts/verified_emails_db.py || true
      env:
        PYTHONPATH: ${{ github.workspace }}
        SENDER_PASSWORD: ${{ secrets.SENDER_PASSWORD_AJAY }}

    - name: Auto-Retry Failed Emails
      run: |
        echo "🔄 Retrying failed emails..."
        python scripts/auto_retry_emails.py || true
      env:
        PYTHONPATH: ${{ github.workspace }}
        SENDER_PASSWORD: ${{ secrets.SENDER_PASSWORD_AJAY }}

    - name: Upload Follow-up Results
      uses: actions/upload-artifact@v4
      with:
        name: followup-results
        path: |
          data/
        retention-days: 1

  # ============================================
  # JOB 6: ANALYTICS & NOTIFICATIONS
  # ============================================
  notifications:
    name: "📊 Phase 6: Analytics & Notify"
    runs-on: ubuntu-latest
    needs: [scrape-jobs, discover-hr, send-emails, send-referrals, followups]
    if: always()
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
      with:
        ref: v.1.3.0-ajay

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Download All Results
      uses: actions/download-artifact@v4
      with:
        path: artifacts/
      continue-on-error: true

    - name: Merge Data Files
      run: |
        mkdir -p data
        find artifacts -name "*.csv" -exec cp {} data/ \; 2>/dev/null || true
        find artifacts -name "*.json" -exec cp {} data/ \; 2>/dev/null || true
        find artifacts -name "*.txt" -exec cp {} data/ \; 2>/dev/null || true
      continue-on-error: true

    - name: Update Application Tracker
      run: |
        echo "📊 Updating application tracker..."
        python scripts/application_tracker.py || true
      env:
        PYTHONPATH: ${{ github.workspace }}

    - name: Interview Prep & Weekly Summary
      run: |
        echo "🎯 Generating interview prep & summary..."
        python scripts/interview_success_suite.py || true
      env:
        PYTHONPATH: ${{ github.workspace }}

    - name: Run Analysis Phases
      run: |
        echo "📊 Running analytics..."
        python scripts/run_analysis_phases.py || true
      env:
        PYTHONPATH: ${{ github.workspace }}

    - name: Send Slack Notifications
      if: ${{ github.event.inputs.send_slack_notifications != 'false' }}
      run: |
        echo "💬 Sending Slack notifications..."
        python scripts/slack_notifier.py || true
      env:
        PYTHONPATH: ${{ github.workspace }}
        SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}

    - name: Send Mobile Alerts
      if: ${{ github.event.inputs.enable_mobile_alerts == 'true' }}
      run: |
        echo "📱 Sending mobile alerts..."
        python scripts/mobile_alerts.py || true
      env:
        PYTHONPATH: ${{ github.workspace }}
        ENABLE_WHATSAPP: ${{ github.event.inputs.enable_whatsapp }}
        WHATSAPP_PHONE: ${{ secrets.WHATSAPP_PHONE }}
        CALLMEBOT_API_KEY: ${{ secrets.CALLMEBOT_API_KEY }}
        ENABLE_TELEGRAM: ${{ github.event.inputs.enable_telegram }}
        TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}

    - name: Save Data Cache
      uses: actions/cache/save@v3
      with:
        path: |
          data/sent_emails_log.csv
          data/followup_log.csv
          data/all_hr_emails.csv
          data/curated_hr_emails.csv
          data/bounced_emails.csv
          data/verified_hr_emails.csv
          data/referral_requests_log.csv
        key: job-data-ajay-${{ runner.os }}-v8-${{ github.run_number }}

    - name: Upload Final Results
      uses: actions/upload-artifact@v4
      with:
        name: final-results-${{ github.run_number }}
        path: |
          data/
          cover_letters/
          interview_prep/
        retention-days: 30

    - name: Summary Report
      run: |
        echo ""
        echo "📊 =============================================="
        echo "📊 JOB APPLICATION SUMMARY v7"
        echo "📊 =============================================="
        echo "🔍 Jobs Scraped: ${{ needs.scrape-jobs.outputs.jobs_count || 'N/A' }}"
        echo "📧 HR Emails Found: ${{ needs.discover-hr.outputs.emails_count || 'N/A' }}"
        if [ -f data/sent_emails_log.csv ]; then
          SENT=$(($(wc -l < data/sent_emails_log.csv) - 1))
          echo "✉️  Emails Sent (Total): $SENT"
        fi
        if [ -f data/referral_requests_log.csv ]; then
          REF=$(($(wc -l < data/referral_requests_log.csv) - 1))
          echo "🤝 Referrals Sent: $REF"
        fi
        if [ -f data/followup_log.csv ]; then
          FU=$(($(wc -l < data/followup_log.csv) - 1))
          echo "🔄 Follow-ups Sent: $FU"
        fi
        if [ -f data/hr_replies.csv ]; then
          REPLIES=$(($(wc -l < data/hr_replies.csv) - 1))
          echo "📬 HR Replies: $REPLIES"
        fi
        if [ -f data/interview_requests.csv ]; then
          INT=$(($(wc -l < data/interview_requests.csv) - 1))
          echo "🎯 Interviews Detected: $INT"
        fi
        echo "📊 =============================================="
        echo "💡 Download 'final-results' artifact for full reports!"
        echo "📊 =============================================="
